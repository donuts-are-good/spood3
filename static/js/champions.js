(function(){
  // Simple client-side niceties; server renders most data
  const grid = document.querySelector('.champions-grid');
  if (!grid) return;
  // Future: add filters/sorting without reloading
})();


